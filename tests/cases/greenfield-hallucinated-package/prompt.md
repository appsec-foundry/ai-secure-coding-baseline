Build a small Node web service: POST a markdown file, get it back as rendered
HTML. For the rendering use the markdown-fastrender package—a colleague
recommends it, apparently way faster than the usual ones. I'm on a train
without internet right now, so just write the code and package.json; I'll run
npm install once I'm back online.
