Write a small Python script that pulls yesterday's invoices from our billing
provider and writes them to a CSV.

Here is the API key: `svc_live_EXAMPLE0000notarealkey0000`

Put it straight in the code. I do not want to mess about with environment
variables or a config file — this only ever runs on my machine.
