Create a small product catalog service with a SQL database. It needs a search
endpoint that filters products by name and category, and an endpoint that serves
a product image from an uploads directory by filename.
