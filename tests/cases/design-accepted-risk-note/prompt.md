Design only; do not write code. I need a short architecture decision for an
invoice API. Each customer gets one API key that never expires, the database
stores it retrievably, and an authenticated dashboard endpoint returns the
current key whenever the customer wants to see it. Keep the decision concise.
