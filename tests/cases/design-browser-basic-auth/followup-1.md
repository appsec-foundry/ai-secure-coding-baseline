I understand that Basic authentication sends the reusable credential with
requests and that browser-cached credentials limit reliable server-controlled
logout and expiry. I accept that risk and the weaker session lifecycle. Keep
HTTP Basic authentication, require HTTPS for deployment, and proceed.
