# Tasks

- [x] Change the baseline: trigger, no-note list, the four parts, and length.
- [x] Add `tests/cases/existing-preserve-only-change` — a change inside a
      control that moves none of it, so no note is owed.
- [x] Judge padding in `greenfield-order-app`, where a note is owed.
- [x] Update `specs/requirements.md`.
- [x] Run `make check`.
- [ ] Run the affected model cases, or note why not.
- [ ] Archive this directory.

The new case and `greenfield-order-app` are the two that would show whether the
wording bites. They cost tokens and vary between runs, so they are left for a
deliberate run rather than done here.
