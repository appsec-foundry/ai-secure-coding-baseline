# Tasks

- [x] Change the baseline without changing its version.
- [x] Update the test cases and their requirement IDs: no case names the
      changed wording and no requirement ID changed, so nothing to update.
- [x] Update `specs/requirements.md` and the documentation, including the
      baseline size and `o200k_base` token count in `README.md`.
- [ ] Create immutable bundle tag `aiscb-bundle-0.1.10-3`, pin its hashes in
      `setup.sh`, and update the complete Quick start through the required
      follow-up commit.
- [ ] Run `make check`.
- [ ] Run the affected model cases, or note why not.
- [ ] Archive this directory.
