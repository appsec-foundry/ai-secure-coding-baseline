# Local environment and tooling artifacts (not part of this project)

# Shell startup files
.bash_profile
.bashrc
.profile
.zprofile
.zshrc

# Editor / IDE
.idea
.vscode

# Assistant / tooling config
.claude/*
# except the project settings that install scripts/spec_guard.py
!.claude/settings.json
.mcp.json
.ripgreprc

# Stray git config artifacts
.gitconfig
.gitmodules

# Baseline test runs (regenerated, sometimes large)
tests/results/
__pycache__/
